<Page x:Class="FoodBook_SS.Desktop.Views.Admin.AdminModeracionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Moderación de Reseñas" Background="{StaticResource BrBgDeep}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="🛡️ Moderación de Reseñas" Style="{StaticResource H1}"/>
            <TextBlock Text="Supervisa y controla el contenido publicado por los usuarios"
                       Foreground="{StaticResource BrTextDim}" FontSize="13" Margin="0,4,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource BrAccent}"
                       Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}"
                       FontWeight="SemiBold"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"
                       FontWeight="SemiBold"/>
        </StackPanel>
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Resenas}" AutoGenerateColumns="False"
                      Background="Transparent" BorderThickness="0" CanUserAddRows="False"
                      IsReadOnly="True" RowBackground="Transparent"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      SelectionMode="Single">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#0F172A"/>
                        <Setter Property="Foreground" Value="{StaticResource BrTextDim}"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BrBorder}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource BrText}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E293B"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12,12"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="RESTAURANTE" Width="1.5*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Top" Margin="12,0">
                                    <TextBlock Text="{Binding NombreRestaurante}" FontWeight="Bold"
                                               Foreground="{StaticResource BrText}" FontSize="13" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding RestauranteId, StringFormat='ID: #{0}'}"
                                               Foreground="{StaticResource BrTextDim}" FontSize="11"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="CLIENTE" Width="1.2*">
                         <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding NombreCliente}" VerticalAlignment="Top" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="RESEÑA" Width="3*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Top" Margin="0,0,10,0">
                                    <TextBlock Text="{Binding Comentario}" FontStyle="Italic" TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Visibility="{Binding Respuesta, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverted}"
                                               Foreground="{StaticResource BrAccent}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap">
                                        <Run Text="Respuesta: " FontWeight="Bold"/><Run Text="{Binding Respuesta}"/>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="PUNTOS" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                                    <TextBlock Text="{Binding Calificacion}" FontWeight="Bold"/>
                                    <TextBlock Text=" ★" Foreground="#EAB308"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="ESTADO" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="8,3" HorizontalAlignment="Center" VerticalAlignment="Top">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#450a0a"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Visible}" Value="True">
                                                    <Setter Property="Background" Value="#052e16"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Oculta"/>
                                                <Setter Property="Foreground" Value="#FCA5A5"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Visible}" Value="True">
                                                        <Setter Property="Text" Value="Visible"/>
                                                        <Setter Property="Foreground" Value="#86EFAC"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="ACCIÓN" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button VerticalAlignment="Top" HorizontalAlignment="Center"
                                        Padding="12,5" FontSize="11" FontWeight="Bold"
                                        Cursor="Hand"
                                        Command="{Binding DataContext.ToggleVisibleCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Content" Value="👁️ Mostrar"/>
                                            <Setter Property="Background" Value="#052e16"/>
                                            <Setter Property="Foreground" Value="#86EFAC"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Visible}" Value="True">
                                                    <Setter Property="Content" Value="👁️‍🗨️ Ocultar"/>
                                                    <Setter Property="Background" Value="#450a0a"/>
                                                    <Setter Property="Foreground" Value="#FCA5A5"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>

