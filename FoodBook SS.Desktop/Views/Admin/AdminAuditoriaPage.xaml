<Page x:Class="FoodBook_SS.Desktop.Views.Admin.AdminAuditoriaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Historial de Auditoría" Background="{StaticResource BrBgDeep}">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="📜 Registro de Auditoría Global" Style="{StaticResource H1}"/>
            <TextBlock Text="Seguimiento de todas las transacciones y cambios en el sistema"
                       Foreground="{StaticResource BrTextDim}" FontSize="13" Margin="0,4,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"
                       FontWeight="SemiBold"/>
        </StackPanel>
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False"
                      Background="Transparent" BorderThickness="0" CanUserAddRows="False"
                      IsReadOnly="True" RowBackground="Transparent"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      RowHeight="40" SelectionMode="Single">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#0F172A"/>
                        <Setter Property="Foreground" Value="{StaticResource BrTextDim}"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BrBorder}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource BrText}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E293B"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="12,8"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Background" Value="Transparent"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="FECHA / HORA" Binding="{Binding FechaHora, StringFormat='{}{0:dd/MM/yyyy HH:mm:ss}'}" Width="150" />
                    <DataGridTextColumn Header="ACTOR" Binding="{Binding ActorId, StringFormat='#{0}'}" Width="70" />
                    <DataGridTemplateColumn Header="ACCIÓN" Width="180">
                         <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                 <Border Background="#334155" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Accion}" FontSize="11" Foreground="#CBD5E1"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="ENTIDAD" Binding="{Binding Entidad}" Width="120" />
                    <DataGridTemplateColumn Header="ID" Width="60">
                         <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#1E293B" CornerRadius="2" Padding="4,1" HorizontalAlignment="Left" VerticalAlignment="Center" BorderBrush="#334155" BorderThickness="1">
                                    <TextBlock Text="{Binding EntidadId}" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource BrAccent}"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="ESTADO" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="✔" Foreground="#22C55E" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding EsExito}" Value="False">
                                                    <Setter Property="Text" Value="✖"/>
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="DETALLE" Binding="{Binding Detalle}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                <Setter Property="ToolTip" Value="{Binding Detalle}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>

